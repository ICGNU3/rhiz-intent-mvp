# Stripe Configuration
# Get these from https://dashboard.stripe.com/apikeys

# Secret key for server-side operations
STRIPE_SECRET_KEY=sk_test_...

# Publishable key for client-side operations (optional, not used in current implementation)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Webhook signing secret
# Get this from https://dashboard.stripe.com/webhooks after creating a webhook endpoint
STRIPE_WEBHOOK_SECRET=whsec_...

# Price IDs for your products
# Create these in Stripe Dashboard > Products
# Root Alpha: One-time payment of $777
STRIPE_ROOT_ALPHA_PRICE_ID=price_...

# Root Beta: Subscription with $75 initial, $75 after 6 months
# Set up as subscription with trial period
STRIPE_ROOT_BETA_PRICE_ID=price_...

# Webhook endpoint configuration:
# 1. Go to https://dashboard.stripe.com/webhooks
# 2. Add endpoint: https://rhiz.app/api/stripe/webhook
# 3. Select events to listen for:
#    - checkout.session.completed
#    - payment_intent.succeeded
#    - customer.subscription.created
#    - customer.subscription.updated
#    - customer.subscription.deleted
#    - invoice.payment_succeeded
#    - invoice.payment_failed